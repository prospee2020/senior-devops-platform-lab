# Pinned 2026-08-21 (resolved + import-tested during course generation).
# Re-pin deliberately; never float in a production image.
fastapi==0.141.1
uvicorn[standard]==0.46.0
prometheus-client==0.26.0
