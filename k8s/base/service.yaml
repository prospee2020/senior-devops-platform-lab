apiVersion: v1
kind: Service
metadata:
  name: senior-devops-api
  labels:
    app.kubernetes.io/name: senior-devops-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: senior-devops-api
  ports:
    - name: http
      port: 80
      targetPort: http
