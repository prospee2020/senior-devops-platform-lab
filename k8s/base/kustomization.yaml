apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

# 'labels' (not the deprecated 'commonLabels') is current Kustomize syntax.
labels:
  - pairs:
      app.kubernetes.io/part-of: senior-devops-platform-lab
